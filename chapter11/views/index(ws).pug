extends layout

block content
  div F12를 눌러 console 탭과 network 탭을 확인하세요.
  script.
    var webSocket = new WebSocket("ws://localhost:8005"); // 연결할 서버 주소; 프로토콜은 ws
    webSocket.onopen = function () { // 서버가 열리면
      console.log('서버와 웹소켓 연결 성공!');
    }
    webSocket.onmessage = function (event) { // 서버에서 메시지가 들어오면
      console.log(event.data);
      webSocket.send('클라이언트에서 서버로 답장을 보냅니다'); // 서버로 메시지 보내기
    }